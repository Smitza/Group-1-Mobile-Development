import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'group.1.mobile.development',
  appName: 'group-1-mobile-development',
  webDir: 'www/browser'
};

export default config;
